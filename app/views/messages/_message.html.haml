.message
  .message__upper-info
   
    %p.message__upper-info__talker
      = message.user.name

    %p.message__upper-info__data
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  - if message.content.present?    
    %p.message__taxt
      = message.content
      = image_tag message.image.url, class: 'message__upper-info__image' if message.image.present?